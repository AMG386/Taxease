<div class="card col-span-{{ isset($span) ? $span : 1 }} mb-2">
    <div class="card-body">
        <p class="text-xs text-gray-500">{{$label}}</p>
        <h5 class="text-xs tracking-wider font-extrabold">{{ $value }}</h5>
    </div>
</div>
